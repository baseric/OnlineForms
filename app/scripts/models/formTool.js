define(['underscore',
		'backbone'
], function ( _, Backbone ) {
	"use strict";

	var FormToolModel = Backbone.Model.extend({

		initialize: function () {
			//console.log('FormToolModel: initialize()');
		}

	});

	return FormToolModel;
});